# 电脑室学生积分管理系统需求文档 v1.0

## 一、产品目标与交付范围

为一位信息技术教师管理多个班级，围绕真实机房座位完成学生积分、课堂展示和卫生管理。学生无需登录。

- **使用端**：以 Windows 浏览器为主；手机浏览器、微信内网页具备完整功能。微信小程序留待后续开发。
- **部署端**：现有 Ubuntu 云服务器，至少 2 核 4GB，使用 Docker Compose，数据集中保存在云端。
- **试运行**：先用公网 IP＋端口访问，仅使用测试数据；接入域名和可信 HTTPS 后，再录入真实学生信息。
- **第一阶段**：登录、班级与学期、名单导入、座位布局和换座、积分操作、时间线、审计及备份。
- **第二阶段**：普通标记、卫生管理、排行榜与动态回放、全屏展示、随机点名、倒计时及完整业务导出。
- 首版不包含多教师协作、学生账号、考勤系统、作业管理、小组积分和完整离线编辑。

## 二、学生、座位与积分

### 班级与学期

- 班级长期存在，全账号统一建立和切换积分学期。
- 新学期积分从 0 开始；学生、座次、普通标记和未结束的卫生任期延续。
- 旧学期只读，可查询和导出；首次启用不导入历史积分。
- 学生字段为学号、姓名、可选备注；学号在班级内唯一，系统内部使用不可变学生 ID。
- 在班学生必须绑定一个独立座位，不设待排座区。超员前必须先增加座位。
- 学生离班采用停用：释放座位、退出当前榜单和点名，结束卫生任期并记录原因，保留身份和历史。
- 停用与匿名化分开。匿名化另行确认，去除姓名、学号和身份备注，历史数值及事件保留；班级通过归档退出日常管理。

### 教室布局与编号

一套共享机房布局，各班保存独立座次。

| 实际列号 | 座位数量 | 编号规则 |
|---|---:|---|
| ①列，屏幕最右侧 | 14 | 从讲台向后：1–14 |
| ②列 | 14 | 从后方向讲台：15–28 |
| ③列 | 13 | 从讲台向后：29–41 |
| ④列，屏幕最左侧 | 13 | 从后方向讲台：42–54 |

- 讲台在图上方，右侧标门，屏幕从左到右为④③②①列。
- ①②列共用一张长桌，③④列共用另一张；每桌两侧电脑背对背，中间为②③列之间的过道。
- 卡片显示座位号、姓名、当前分数与标记，同时表达电脑和学生的朝向。
- 支持在四列内任意位置插入座位、调整位置、删除空座。新增编号使用历史最大编号加一，已有编号不变、不复用。
- 移动实体机位时，各班学生跟随机位移动；删除机位前检查所有班级，仍有人占用则禁止删除。
- 全局布局变更提交前预览影响，记录审计。积分记录保存操作时的位置快照。

### 选择与换座

- 普通模式支持单击单选、鼠标拖框、整列选择、全班选择；选中后显示底部操作栏，并预留空间，不遮挡内容。
- 默认补充 Windows 式选择规则：`Ctrl` 增减选择，空白处清空，`Esc` 取消选择。
- 换座进入独立模式，避免拖框选择与拖动学生冲突。
- 单人可互换；批量保持相对位置，目标位置原有学生交换到腾出的位置。
- 新旧区域部分重叠时按移动链轮换；越界或缺少目标座位时不能提交。
- 换座先在草稿中预览，保存时整体提交；取消恢复原座次。
- 手机提供选择学生、选择目标座位、交换或批量移动的明确入口，不依赖精细拖拽。
- 电脑默认适配完整座位图；手机支持缩放、平移，并可切换列表完成管理操作。

### 名单导入

- 提供两种固定 Excel 模板：标准行表、仿教室平面座位表。
- 行表字段为学号、姓名、座位号、备注；平面模板每个机位提供学号和姓名填写格，避免仅凭姓名识别学生。
- 导入先解析、校验、展示变更预览，再一次性提交；错误定位到工作表及单元格或行。
- 按班内学号更新已有学生，新学号新增；未出现在文件中的学生保留，积分与标记保留。
- 重复学号、重复占座、不存在的座位、超员，以及与保留学生的座位冲突均阻止提交。
- 学生导出包含可重新导入的名单和座位格式。

### 积分与追溯

- 分数为整数，允许负分；加减分必须是非零整数。
- 原因模板保存名称、固定正负方向和默认分值；当次可改大小，不能反转模板方向。
- 全局模板可由班级新增、隐藏或覆盖，班级调整不影响其他班级。
- 可不选原因；提供 `+1、+2、-1、-2` 快捷值和自定义整数输入。
- 单人点击模板或快捷值立即提交；需要改值时进入自定义操作。批量先展示人数、分值和原因，再确认提交。
- 提交成功清空选择，显示结果和撤销入口；重复点击、请求重试不能重复记分。
- 一次批量操作形成一个批次，每名学生各有一条明细，整体成功或整体失败。
- 撤销生成关联原记录的反向记录，原记录不可改写或删除。支持单人撤销、整批撤销；已撤销明细不得再次冲销。
- 只按服务器实际提交时间记录，不能回填发生时间；补录缘由可写在备注中。
- 提供全班流水和学生详情，可按学期、日期、学生、加减方向和原因筛选；记录包含分值、前后分数、原因快照、批次及撤销关系。

## 三、卫生管理员与课堂展示

### 普通标记

支持自定义名称、图标和颜色，一人可有多个标记。卫生管理员是独立业务角色，通过卫生流程管理，不能仅靠添加普通图标改变任职状态。

卫生管理员卡片使用扫帚图标和琥珀色高亮，显示任职状态与“完成次数／应完成次数”；选中样式使用不同颜色，保证两者可区分。

### 卫生管理员规则

**核心顺序：先完成本轮打扫，再从符合条件的完成人中抽选替补。**

1. 每班独立开展卫生检查，保留本轮开始时的原管理员名单，检查状态可跨刷新、跨设备继续。
2. 登记本轮再次未推椅子的原管理员；其任期和完成进度保留，但本轮不能被随机替补。
3. 勾选实际参加打扫的原管理员，批量计一次，同轮同人最多一次。
4. 完成次数达到应完成次数者先自动退役，不占用替补名额。
5. 从其余实际完成打扫、仍在任且本轮有替补资格的原管理员中，冻结候选名单。
6. 逐个录入新发现的未推椅子学生，系统不放回随机抽选候选管理员；先预览，再确认，确认后原管理员退役。
7. 新任学生从**下一轮**开始值日，本轮不计完成次数，也绝不进入本轮替补候选名单。
8. 候选名单用尽或原本无人符合条件时，继续发现的学生直接新增为管理员。

补充规则：

- 每个新任期默认从 `0/3` 开始，重复任职重新计数，历次任期保留。
- “应值日却未参加打扫”经老师确认后，应完成次数加一，例如 `1/3 → 1/4`；同轮最多增加一次。
- 未参加值日者保留任期，本轮不能被替补，也不能计完成次数。
- 普通课堂缺席不自动增加卫生义务，与随机点名的临时排除名单独立。
- 已在任者再次未推椅子只取消本轮替补资格；实际完成打扫仍正常计次，达到应完成次数仍可退役。
- 随机结果保存在服务器；取消预览不重抽，同一待处理学生再次打开仍显示原结果。候选状态被修正导致结果失效时，明确说明并留痕。
- 同一班级同时最多一个未结束检查轮次；界面区分“本轮值日人员”和“下一轮新任人员”。
- 可填写理由手动解除、恢复任期或纠正次数，保留前后状态及操作记录；不得通过重复抽签代替纠错。
- 卫生操作不自动加减积分。

### 排行榜与动态回放

- 普通榜单默认当前班级、当前学期，可切换全部班级；跨班显示班级信息。
- 同分采用并列名次，默认按 `1、1、3` 展示；较早达到当前分数者在前，时间相同再按班级和学号稳定排序。
- 动态视图为横向柱状图竞赛，随着事件变化上下换位，展示当时 Top10，而非只跟踪最终前十名。
- 支持日期范围、播放、暂停、倍速与拖动时间轴。
- 默认按学期累计分回放，可切换区间净增减分；累计模式包含区间起点已有分数。
- 同一次积分批次作为一个时间状态变化，避免人为产生逐人排名先后。
- Top10 最多十人，同分按统一规则截取；负分以零线左侧柱子表示。
- 历史回放按当时在班状态显示成员，离班前仍参与，离班后退出；匿名化学生显示匿名代号。

### 展示与课堂工具

- 提供已登录状态下的独立全屏展示模式，展示座位、榜单或点名结果；显示座位号＋姓名，隐藏私人备注和管理入口。
- 管理数据变化自动同步其他设备和展示页面，不提供无需登录的公开展示链接。
- 随机点名支持全班或选中范围、临时排除缺席者、一次抽多人、本轮不重复；抽完后由老师开启新一轮。
- 倒计时支持自定义时长、暂停、继续、重置及到时提示。
- 视觉采用浅色背景、蓝绿色强调色，优先保证姓名、分数与操作状态可读。

## 四、技术、数据与部署约定

以下为实施默认方案，不增加业务范围。

- **技术架构**：React＋TypeScript 响应式前端，Node.js＋TypeScript 后端，PostgreSQL 数据库；Docker Compose 编排应用、数据库及反向代理。
- **接口边界**：提供版本化 API，覆盖班级/学期、学生、机位/座次、积分批次与撤销、模板/标记、卫生检查与任期、榜单回放、课堂工具、导入导出及审计；生成接口说明，为小程序复用后端保留条件。
- **关键数据对象**：学生身份与班级关系、稳定机位 ID 与显示编号、学期积分账本、批次和撤销关联、卫生轮次与冻结候选名单、任期完成量与应完成量、成员状态历史。
- **一致性**：积分余额与账本同步事务提交；换座、导入、随机替补及学期切换使用事务和版本校验。所有写入以服务端校验为准。
- **多端同步**：使用服务端事件通知刷新数据。积分追加可顺序合并；旧版本页面的换座、卫生和学期修改拒绝提交，提示载入最新状态。
- **断网**：保留当前页面已加载内容并明确标记离线，禁止修改；恢复连接后刷新。首版不保证断网重新打开页面可用。
- **登录**：唯一教师账号，用户名＋密码，支持改密和退出其他设备；忘记密码通过服务器维护命令重置。
- **安全与审计**：数据库不对公网开放；使用密码哈希、登录限流、访问控制与上传校验。记录登录和所有管理状态变更，敏感凭据不进入日志。
- **时间**：服务器统一记录时间，界面默认按北京时间显示。
- **保留与备份**：业务历史永久保留，支持归档与身份匿名化；每天完整备份，保留最近 30 天，界面显示成功时间或失败状态并支持下载，恢复通过维护命令执行。
- **匿名化边界**：现存历史快照和展示缓存一并处理；下载文件、尚未到期的旧备份可能仍含原身份，恢复旧备份后需先重新应用后续匿名化记录再开放使用。
- **业务导出**：Excel 名单与座位、可筛选的积分明细、排行榜；完整数据库备份与业务导出分开。
- **默认几何细节**：四列前端与讲台方向对齐，门先作右侧方位标识；不模拟未提供的精确建筑尺寸。

## 五、验收与分阶段上线

### 第一阶段验收

- 两种名单模板可正确导入蛇形编号的 54 个机位；重复导入保留历史，冲突时不产生部分写入。
- 所有在班学生始终各占一个座位；共用布局删除操作能检查全部班级。
- 框选、整列、全班选择及单人/批量换座正常；重点覆盖重叠移动链、空座、不同列长度、越界和取消预览。
- 单人快捷记分、批量确认、自定义无原因、负分、重复请求、部分撤销后整批撤销均正确，余额与流水一致。
- 全局切换学期后积分归零，座次及需延续状态保留，历史只读。
- 电脑和手机具备本阶段完整功能；双设备冲突明确提示，断网不接受修改。
- 在约定测试机上，54 座选择和拖动无明显卡顿，本地交互反馈目标不超过 100 毫秒；正常网络下提交结果及多端同步目标不超过 2 秒。
- 完成一次备份恢复演练，再进入真实数据上线准备；真实名单启用前完成 HTTPS 配置。

### 第二阶段验收

- 卫生检查严格遵循“完成打扫→达标退役→冻结候选→随机替补”，新任者不进入本轮抽选。
- 覆盖候选耗尽、取消预览不重抽、刷新继续、两设备重复确认、再次违规、未值日加义务和人工纠正。
- 特别验证 `1/3 → 1/4`，以及新增管理员下一轮才开始计次。
- 动态榜单正确处理并列、负分、整批变更、撤销事件、跨班比较、历史离班状态及匿名化。
- 点名不重复，排除名单生效；倒计时可暂停继续；展示模式隐藏管理内容并同步更新。
- 导出内容与筛选结果一致，导出的座位表可重新导入。

完成后形成可实施的需求基线、接口说明、部署与恢复说明，以及上述场景对应的验收清单。
