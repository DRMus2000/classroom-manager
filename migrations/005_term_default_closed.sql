-- =====================================================================
-- 电脑室学生积分管理系统 · 迁移 005
-- 覆盖：新建学期默认 closed，避免和唯一 open 学期冲突
-- 依赖：001_phase1_core.sql
-- 幂等：可重复执行
--
-- 同一时间只能有一个 open 学期。创建接口写入 closed，
-- 由激活操作把它打开并关掉原来的学期。
-- =====================================================================

ALTER TABLE term ALTER COLUMN status SET DEFAULT 'closed';
