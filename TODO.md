# 实施计划

依据是 `docs/DESIGN.md`、`docs/SCHEMA.md`、`docs/API.md`。已在仓库里落地的步骤标为完成。下一轮从第一个未勾选项开始，不跳着做后面的接口。

## 1. 脚手架与基础配置

- [x] Node 22 + TypeScript + Fastify 后端工程，以及 React 18 + Vite 前端工程骨架
- [x] Docker Compose（nginx、api、postgres）、Nginx、`.env.example`、`.gitignore`
- [x] 固定 Drizzle 与 `db.execute` 的返回类型，使 `npm run typecheck` 能通过
- [x] 在 PostgreSQL 16 上执行 `001`、`002`，确认迁移可重复执行且种子列方向与 `docs/SCHEMA.md` 一致
- [x] 迁移之后调用符合规范的 `renumerate()`，把初始 54 座回填为 1–54

## 2. 数据模型与迁移

- [x] 第一阶段表：账号、班级学期、机位、座次、积分账本、审计、幂等、事件、备份、匿名登记
- [x] 第二阶段表：普通标记、卫生轮次、冻结候选、抽选；含 `frozen_at`、`version` 和未确认抽选唯一索引
- [x] 对照 `docs/SCHEMA.md` 核对主外键、部分唯一索引和三支触发器（四列上限、禁止反转模板方向、旧学期禁写账本）
- [x] 给 `point_balance.last_change_seq` 补一条说明：它存的是批次回放事件序号，不是逐条明细序号

## 3. 核心中间件

- [x] Zod 契约、`AppError` 与错误码到 HTTP 状态的映射
- [x] 会话 Cookie、登录限流、幂等键 `request_id`
- [x] 乐观锁 `expected_version`（座次、新增、离班、恢复）和切学期的 `expected_current_term_id`
- [x] SSE 事件种类，含 `rollcall_changed`；断线用 `Last-Event-ID` 从 `event_log` 补发
- [ ] 写事务提交成功后再广播 SSE，避免事务回滚后客户端已收到事件
- [ ] 写接口统一走幂等包装：同键同体返回原响应，同键不同体返回 409

## 4. 各模块 API

服务已有、路由未接上的，先补路由再改行为。服务还没有的，按领域函数、仓储、服务、路由的顺序做。

- [x] 登录、改密、退出、退出其他设备、当前用户
- [x] 班级、学期、全局切学期、学期汇总
- [x] 学生列表、新增、修改、离班、恢复
- [x] 布局预览与提交、座次读取、换座预览与提交、记分、单条撤销、整批撤销、积分时间线
- [ ] 把名单导入的预览和提交接到 `POST /classes/:id/import/preview` 与 `/commit`，并提供两个 Excel 模板下载
- [ ] 把学生匿名化、班级匿名化接到对应路由；外部账本失败则整次回滚
- [ ] 把普通标记的定义和打标接到 `/marks` 与 `/students/:id/marks/:mark_id`
- [ ] 把审计查询和备份记录接到 `/audit` 与 `/backup/records`
- [ ] 积分模板：全局模板、班内覆盖、班内私有模板
- [ ] 卫生服务：开始轮次、计次、未推椅子、`1/3 → 1/4`、冻结候选、抽选、取消不重抽、确认、纠正失效、关轮
- [ ] 排行榜 `GET /leaderboard`，并列名次为 `1, 1, 3`
- [ ] 回放：时间轴、帧、任意时刻状态；检查点每 200 个回放事件加每日兜底，不进入记分事务
- [ ] 点名与倒计时，并用 SSE 同步到其他端
- [ ] Excel 导出：花名册可再导入，积分明细和榜单与列表筛选一致
- [ ] 前端页面：座位图、手机三步换座、记分、卫生、榜单回放、全屏展示、离线禁写

## 5. 运维脚本

- [x] 迁移命令，以及创建教师、重置密码、重算余额
- [ ] 回放检查点任务
- [ ] 每日 `pg_dump` 记录与失败告警
- [ ] 匿名化账本导出失败后的重试，以及恢复旧备份后的补做命令

## 6. 测试

- [x] 编号蛇形结果、换座重叠轮换、卫生候选与 `1/3 → 1/4` 的纯函数测试
- [ ] 导入冲突时整单不写入
- [ ] 重复 `request_id` 不重复记分；部分撤销后再整批撤销只冲销剩余明细
- [ ] 换座版本过期返回 409，不覆盖另一台设备的座次
- [ ] 卫生：取消预览不重抽、双设备重复确认、新任者不进本轮候选
- [ ] 在测试库上跑通 `001` 和 `002` 后再做上述集成测试
