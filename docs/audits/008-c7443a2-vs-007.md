# 代码审查报告 008

- 编号：008
- 日期：2026-09-25
- 范围：对照 [007](2026-09-25-codebase-audit.md)，检查提交 `c7443a2`
- 结论：**[CHANGES REQUESTED]**

007 的高风险项在代码路径上已经接到文档描述的做法。目标机上的备份恢复演练仍然没有做。`server.ts` 没有拆开，CI 也没有跑数据库集成测试。

## 对照结果

| 007 | 状态 | 依据 |
|---|---|---|
| H1 镜像里没有 TypeScript | 已修复 | `Dockerfile` 先在完整依赖阶段 `npm ci` 并 `npm run build`，运行阶段再 `--omit=dev`。运行镜像安装了 `postgresql16-client`。CI 的 `image` 作业会 `docker build`。 |
| H2 备份无法写盘、没有调度 | 代码已接上，演练未做 | 备份容器把 `backups` 卷挂到 `/backups`，没有只读。`scripts/backup-scheduler.ts` 每天 02:15（Asia/Shanghai）调用 `pg_dump`。API 提供 `GET /backup/download/:backup_id`。`restore` 只能指向另一个库。007 要求的真实 dump 恢复演练没有出现在这次提交里。 |
| H3 硬编码密码、Cookie 不带 Secure | 已修复 | Compose 用 `POSTGRES_PASSWORD` 组装两边的连接串。`HTTPS_ENABLED` 默认 `true`，登录和改密都走 `sessionCookieOptions()`。Nginx 监听 443，80 跳到 HTTPS，并代理 `/api/` 与 `/api/v1/events`。没有证书时入口脚本会生成 localhost 自签证书。 |
| H4 账本先于数据库提交 | 已修复 | `anonymizeStudent` 先 `commitAnonIntent` 提交库内意图，再写文件，最后把导出标成 `exported`。文件失败时意图留在 `pending`，不再整笔回滚。 |
| M1 预览解析结果不释放 | 已修复 | `cleanupPreviews` 调用 `dropExpiredKeys`，同时删除令牌和 `payloadChangeSets`。 |
| M2 跨班或隐藏模板可记分 | 已修复 | 记分前用本班有效模板集查找 `template_id`，隐藏或不属于该班则 `FORBIDDEN`。 |
| M3 代理后 IP 与并发限流 | 已修复 | `TRUST_PROXY=1`，只信任一跳。失败计数和失败插入在同一事务里，并用 `pg_advisory_xact_lock` 按用户名和 IP 串行。 |
| M4 时间线按明细切开批次 | 已修复 | `listTimeline` 先按批次分页，再取出这些批次的全部明细。 |
| M5 回放每页从头重算 | 已缓解 | `replayResumeSeq` 在有游标时从该序号附近的检查点补算，不再每次都从区间起点重放整段。 |
| M6 路由全集中、缺下载和 OpenAPI | 部分修复 | 备份下载已实现。`package.json` 里不存在会失败的 `openapi` 脚本。`src/server.ts` 仍然是单一入口，没有按模块拆开。 |
| M7 记分备注被丢掉 | 已修复 | 迁移 `004_point_batch_note.sql` 增加 `note`。请求、写入、时间线和导出都带这个字段。 |
| L1 未使用的前端依赖 | 已修复 | `web/package.json` 中已没有 TanStack Query 和 Zustand。 |
| L2 `as any` 与过宽契约 | 未处理 | `points.ts`、`layout.ts`、`imports.ts` 里仍有 `as any`。 |
| L3 没有 CI | 部分修复 | 新增 `.github/workflows/ci.yml`，只跑类型检查、一组纯函数测试和镜像构建。嵌入式 PostgreSQL 集成测试不在流水线里。 |
| L4 没有 LICENSE | 已修复 | 仓库根目录有 `LICENSE`。 |
| L5 仓储直接广播 SSE | 已修复 | `repo/audit.ts` 的 `writeEvent` 只插入行。`src/services/publishEvent.ts` 在提交后调用 `afterCommit`。各服务改为从这里写入事件。 |

## 还要做的事

在目标机上用 Compose 做一次：迁移、登录并确认 `Set-Cookie` 带 `Secure`、记一笔分、等备份容器写出 dump、下载后 `restore` 到另一个库、再跑匿名补做。CI 里补上隔离端口的数据库集成测试。
